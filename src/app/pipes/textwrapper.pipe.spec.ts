import { TextwrapperPipe } from './textwrapper.pipe';

describe('TextwrapperPipe', () => {
  it('create an instance', () => {
    const pipe = new TextwrapperPipe();
    expect(pipe).toBeTruthy();
  });
});
